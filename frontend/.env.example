# Google OAuth "Web application" client ID, used for the "Sign in with
# Google" button. Must match GOOGLE_CLIENT_ID in backend/.env, and its
# Google Cloud Console entry must list this site's origin under
# "Authorized JavaScript origins".
VITE_GOOGLE_CLIENT_ID=
